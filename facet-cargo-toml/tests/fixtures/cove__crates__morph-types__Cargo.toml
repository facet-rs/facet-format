[package]
name = "morph-types"
version = "0.1.0"
edition = "2024"
description = "Type definitions for morph diff computation states (computed, loading, error)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[lints]
workspace = true
