[package]
name = "nerdfonts"
version = "0.1.0"
edition = "2024"

[features]
default = ["build-script"]
# Enable build script for cargo builds (generates icons.rs from icons.css)
# Disable for buck2 builds (uses pre-generated generated_icons.rs)
build-script = []

[dependencies]

[build-dependencies]
cssparser = "0.35.0"
eyre = "0.6.12"
fs-err = "3.1.1"
