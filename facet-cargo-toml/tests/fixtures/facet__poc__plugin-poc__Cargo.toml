[package]
name = "facet-plugin-poc"
version = "0.1.0"
edition = "2024"
rust-version = "1.89.0"
publish = false

[workspace]

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
unsynn = "0.3.0"

# Use the new facet macro crates
facet-macro-types = { path = "../../facet-macro-types" }
facet-macro-parse = { path = "../../facet-macro-parse" }
