[package]
name = "fabric-dashboard-types"
version = "0.1.0"
edition = "2024"
description = "Shared types for Fabric dashboard - usable by both server and browser"

[dependencies]
remoc = { version = "0.18" }
serde = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
