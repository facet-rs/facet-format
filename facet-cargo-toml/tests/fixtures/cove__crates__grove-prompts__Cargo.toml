[package]
name = "grove-prompts"
version = "0.1.0"
edition = "2024"
description = "Minijinja template-based prompt generation for various agent archetypes and personality types"

[dependencies]
grove-types = { version = "0.1.0", path = "../grove-types" }
grove-archetypes = { version = "0.1.0", path = "../grove-archetypes" }
minijinja = "2.12.0"
serde_json = "1.0"

[lints]
workspace = true
