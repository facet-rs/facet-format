[package]
name = "varnish"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "HTML parsing and rewriting utilities using html5ever for DOM manipulation and content transformation"

[dependencies]
camino = { workspace = true }
html5ever = { workspace = true }
markup5ever = { workspace = true }
markup5ever_rcdom = { workspace = true }
regex = { workspace = true }

[lints]
workspace = true
