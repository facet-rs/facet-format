[package]
name = "grove-server-types"
version = "0.1.0"
edition = "2024"
description = "Shared type definitions and Remoc channel specifications for server-client communication in the Grove architecture"

[dependencies]
agent-client-protocol-schema = { workspace = true, features = ["unstable"] }
anyhow = { workspace = true }
grove-types = { version = "0.1.0", path = "../grove-types" }
remoc = { version = "0.18" }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true, features = ["sync"] }

[lints]
workspace = true
