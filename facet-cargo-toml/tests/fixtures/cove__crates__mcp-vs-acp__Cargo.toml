[package]
name = "mcp-vs-acp"
version = "0.1.0"
edition = "2024"
description = "Schema translation and compatibility utilities between MCP and the Agent Client Protocol"

[dependencies]
agent-client-protocol-schema = "0.6.3"
rust-mcp-schema = "0.7.5"
serde_json = "1.0.145"
tracing = "0.1.41"

[lints]
workspace = true
