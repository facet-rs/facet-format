[package]
name = "directives"
version = "0.1.0"
edition = "2024"
description = "Directive management utilities for handling configuration and instruction directives in Grove"

[dependencies]
grove-types = { version = "0.1.0", path = "../grove-types" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"

[lints]
workspace = true
