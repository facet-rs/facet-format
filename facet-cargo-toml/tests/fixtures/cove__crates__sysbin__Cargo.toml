[package]
name = "sysbin"
version = "0.1.0"
edition = "2024"
description = "System binary locator and cache providing unified interface to find and execute common development tools"

[dependencies]
camino = "1.1"
tokio = { version = "1", features = ["process"] }
thiserror = "2.0"
once_cell = "1.20"
which = "7.0"
parking_lot = "0.12.5"

[lints]
workspace = true
