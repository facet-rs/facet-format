[package]
name = "bash-to-mcp"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Suggest MCP (Model Context Protocol) tool alternatives for shell commands"

[dependencies]
# No dependencies! Pure logic

[lints]
workspace = true
